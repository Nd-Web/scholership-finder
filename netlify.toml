[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NEXT_USE_NETLIFY_EDGE = "true"
  NEXT_PRIVATE_TARGET = "server"

# Set runtime for Next.js
[[plugins]]
  package = "@netlify/plugin-nextjs"
